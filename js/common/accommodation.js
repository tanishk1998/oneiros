$(document).ready(function() {
    $('#fullpage').fullpage({
      scrollBar: true,
      anchors: ['guide', 'conditions', 'rules','rules2']
    })
  
    // //methods
    // $.fn.fullpage.setAllowScrolling(false);
  })
  