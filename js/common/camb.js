$(document).ready(function() {
  $('#fullpage').fullpage({
    scrollBar: true,
    anchors: ['work', 'incentives', 'opportunities']
  })

  // //methods
  // $.fn.fullpage.setAllowScrolling(false);
})
